# Installation
```toml
[[ballsdex.packages]]
location = "git+https://github.com/flaree/ballsdex-collector.git@v0.0.1#master"
path = "collector"
enabled = true
```

# Collector Package

Note, for every new collector type added you must restart the bot so it can register the new commands.